# Voyage 프로젝트 현황

작성일: 2026-08-14

최신 작업 기준: 2026-08-14. Supabase SQL 4개 파일 실행 완료, Vercel 환경변수 등록 및 배포 완료 상태를 전제로 합니다.

## 1. 현재까지 진행한 작업

Voyage는 여행을 `여행 전 → 여행 중 → 여행 후` 단계로 나누어 관리하는 Next.js 기반 웹앱입니다. 현재 코드는 `main` 브랜치에 반영되어 있고, Vercel 배포와 Supabase 연결을 전제로 동작합니다.

### 기본 구조

- Next.js App Router 기반 화면 구조
- 모바일에서는 하단 메뉴, 데스크톱에서는 좌측 메뉴 제공
- 한국어/영어 전환 지원
- 여행별 데이터를 하나의 `TripDataProvider`에서 관리
- Supabase 연결 전에는 localStorage fallback 사용
- 주요 화면 영역에 `data-section` 이름을 부여해 역할을 구분

### 인증

- 이메일 회원가입
- 회원가입 시 비밀번호 확인 입력
- 이메일/비밀번호 로그인
- 비밀번호 재설정 메일
- 비밀번호 변경 후 계정 화면 처리
- Google OAuth 로그인 코드 연결

### 여행 관리

- 사용자가 직접 여행을 생성
- 여행 이름, 국가, 도시, 시작일, 종료일 입력
- 여행 목록에서 직접 추가한 여행만 표시
- 여행별 `before`, `during`, `after` 화면
- 여행 완료 처리
- 여행 상세 요약 카드

### 여행 전

- 준비물 체크리스트
- 완료 항목 숨김/다시 보기
- 준비물 카테고리 추가, 이름 변경, 삭제
- 여행 예약 추가
- 예산 계획과 실제 사용 내역 분리
- 예산 항목 이름, 세부사항, 카테고리, 금액, 통화, 결제수단 관리
- 예산 카테고리와 결제수단 추가/수정/삭제
- 통화별 합계
- 원화 기준 예상 총액
- 무료 환율 API를 이용한 최신 영업일 환율 조회
- 환율 수동 조정

### 여행 중

- 일정 보드
- 날짜·시간·장소별 일정 관리
- 일정 완료 체크
- 일정 완료 항목을 추억 탭으로 자동 연결
- 추억 탭에서 완료 일정과 방문 완료 장소 확인
- 장소 지도 및 장소 상세
- 지출 기록
- 여행자별 지출 정산
- 기념품 목록
- 날씨 화면 구조

### 여행 후

- 총 지출 통계
- 통화별 지출
- 카테고리별 지출
- 여행자별 정산
- 여행 평점과 후기
- 다시 방문할 장소, 좋았던 점, 개선할 점 기록

### 공용 여행 보드

- 현재는 별도 초대 없이 사이트에 접속한 모든 사람이 같은 Supabase 여행 목록을 확인
- 여행, 일정, 장소, 준비물, 예산, 예약 등의 데이터를 함께 수정
- Supabase가 연결된 Vercel 배포 환경에서는 여러 기기에서 같은 데이터 사용
- 개인별 소유권·초대·멤버 권한 기능은 보류

현재 공개 RLS 정책에서는 사이트 방문자가 데이터를 볼 수 있고 수정할 수도 있습니다. 여행 동행자끼리만 사용하는 단계에서는 편리하지만, 공개 서비스로 전환할 때는 반드시 사용자별 RLS로 교체해야 합니다.

## 2. 현재 사용 가능한 기능

### 사용자가 바로 할 수 있는 것

1. 회원가입 또는 로그인
2. `새 여행`에서 여행 생성
3. 여행 전 화면에서 준비물·예약·예산 입력
4. 여행 중 화면에서 일정·장소·지출 입력
5. 일정 완료 체크 후 추억 탭 확인
6. 여행 상세 화면에서 공유 링크 생성
7. 보기 전용 또는 공동 편집 권한 선택
8. 여행 후 화면에서 정산과 후기를 기록

### 데이터 저장 방식

- Supabase 환경변수가 설정되어 있으면 API를 통해 Supabase에 저장됩니다.
- Supabase가 연결되지 않은 경우 localStorage fallback으로 동작합니다.
- 여행 생성 API는 `public.trips`에 insert합니다.
- 여행 하위 데이터는 `trip_id`를 기준으로 저장됩니다.
- 같은 여행의 장소·지출·일정·예산 데이터가 서로 연결됩니다.

## 3. 현재 보완이 필요한 부분

### 권한 보안

현재 `supabase/rls.sql`은 초기 MVP를 위한 공개 anon 정책입니다. 즉, 기능 확인에는 편하지만 실제 서비스용으로는 충분히 안전하지 않습니다.

보완해야 할 내용:

- `trips`에 `owner_id` 또는 `created_by` 추가
- 모든 하위 테이블에 사용자 소유권 연결
- `auth.uid()` 기반 RLS로 전환
- 공유 멤버 테이블과 실제 로그인 사용자 연결
- 보기 전용/편집 권한을 서버에서 검증
- 공유 링크 생성·취소 권한을 소유자에게만 허용

### 공동 편집 범위

현재 공동 편집 링크는 공유 화면에서 일정 완료 상태를 변경하는 기능까지 구현되어 있습니다. 예산, 장소, 예약, 지출까지 공유 화면에서 직접 수정하는 기능은 추가 개발이 필요합니다.

### 이메일 초대

현재 이메일 초대는 자체 이메일 발송 서비스가 아니라 사용자의 기본 메일 앱을 여는 방식입니다. 자동 발송이 필요하면 Resend, SendGrid, Supabase Edge Function 등의 이메일 서비스 연동이 필요합니다.

### 실제 데이터 기반 메인 화면

여행 목록은 사용자가 추가한 여행을 기반으로 동작하지만, 메인 화면의 일부 요약은 아직 단일 대시보드용 fallback 구조가 남아 있습니다. 여러 여행 중 가장 가까운 여행을 자동 선택하고 실제 통계로 메인 카드를 구성하는 작업이 필요합니다.

### 다국어 완성도

주요 화면은 한국어로 제공되지만 일부 하위 화면과 입력 선택지는 영어가 남아 있을 수 있습니다. 새 기능을 추가할 때 문자열을 화면에 직접 쓰지 않고 `lib/i18n.tsx` 사전에 추가하는 방식으로 통일해야 합니다.

### 일정과 추억

현재 추억은 완료한 일정과 방문한 장소를 자동으로 모아 보여주는 구조입니다. 사진 업로드, 감정 기록, 긴 글 작성, 장소별 추억 편집은 아직 추가되지 않았습니다.

## 4. 사용자가 Supabase에서 직접 해야 하는 작업

### 최초 또는 기존 프로젝트에 실행할 SQL

Supabase Dashboard → SQL Editor에서 아래 순서로 실행하세요.

1. `supabase/schema.sql`
2. `supabase/rls.sql`
3. 기존 예산 테이블이 이미 있다면 `supabase/budget-fields.sql`
4. 공유 기능을 사용할 경우 `supabase/share-links.sql`

`supabase/seed.sql`은 샘플 데이터를 넣는 파일이므로, 실제 사용자 데이터만 사용할 경우 실행하지 않는 것을 권장합니다.

### Vercel 환경변수

Vercel 프로젝트의 Development, Preview, Production 환경에 아래 값을 각각 등록해야 합니다.

```env
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
```

환경변수 변경 후에는 Vercel에서 새 배포가 필요합니다.

### Supabase Auth 설정

- Email provider 활성화
- 이메일 인증을 사용할 경우 확인 메일 템플릿 확인
- Site URL에 Vercel 주소 등록
- Redirect URLs에 다음 주소 등록
  - `https://배포주소/auth/callback`
- Google 로그인을 사용할 경우 Supabase Google provider와 Google Cloud OAuth redirect URL 설정

### 배포 후 확인할 기능

- 회원가입 및 이메일 인증
- 로그인 및 로그아웃
- 새 여행 생성
- 여행 목록 재접속 후 데이터 유지
- 예산 항목 저장
- 일정 완료 후 추억 반영
- 공유 링크 생성 및 다른 브라우저에서 열기
- RLS 오류 여부

## 5. 앞으로 추가하면 좋은 기능

### 우선순위 높음

1. 사용자별 `owner_id` 기반 RLS 전환 — 공유 기능 재개 시 진행
2. 공유 멤버 테이블과 로그인 계정 연결 — 공유 기능 재개 시 진행
3. 초대 수락/거절 화면 — 공유 기능 재개 시 진행
4. 공유 멤버별 보기/편집 권한 관리 — 공유 기능 재개 시 진행
5. 공동 편집 변경사항 실시간 반영 — 공유 기능 재개 시 진행
6. 여러 여행 중 가장 가까운 여행을 메인에 자동 표시 — 기본 구현 완료
7. 모바일에서 실제 기기 테스트 및 입력 UX 개선 — 기본 반응형 보완 완료, 실기기 확인 필요

#### 실행 보류 기록

요청에 따라 인증 기반 공유 기능은 당장 확장하지 않고 후속 작업으로 기록했습니다. 현재는 동행자끼리 사용하는 공용 여행 보드 방식으로 운영합니다. 인증 기반 공유를 다시 진행할 때 1번부터 5번을 순서대로 이어서 작업합니다.

### 우선순위 중간

1. 일정 드래그 앤 드롭 정렬
2. 일정별 사진·메모·체크인 기록
3. 추억 게시물 작성 및 수정
4. 사진 업로드용 Supabase Storage 연동
5. 예약 확인서와 티켓 파일 첨부
6. 지출 CSV 내보내기
7. 여행 PDF 또는 공유용 요약 리포트 생성
8. 실제 날씨 API 연동
9. 지도 API 연동 및 실제 지도 마커 표시

### 우선순위 낮음 또는 추후 검토

1. iCloud 로그인
2. 카카오·네이버 로그인
3. 푸시 알림
4. 일정 리마인더
5. 여행 템플릿
6. 여러 통화 자동 정산
7. 오프라인 우선 동기화

## 6. 현재 프로젝트 상태 요약

| 항목 | 상태 |
| --- | --- |
| 로컬 개발 | 가능 |
| Vercel 배포 | 완료 전제 |
| Supabase 연결 | 연결 구조 완료 |
| 이메일 로그인 | 구현 |
| Google OAuth | 코드 연결, provider 설정 필요 |
| 여행 생성 | 구현, Supabase RLS 필요 |
| 예산/실제 지출 분리 | 구현 |
| 최신 환율 조회 | 구현 |
| 일정 완료→추억 연동 | 구현 |
| 공용 여행 보드 | 구현 |
| 개인별 공유 권한 | 보류 |
| 사용자별 보안 RLS | 추가 작업 필요 |
| 자동 초대 이메일 발송 | 추가 작업 필요 |
| 사진 기반 추억 게시판 | 추가 작업 필요 |

## 7. 개발 및 배포 확인 명령

```bash
npm run lint
npm run build
git status
git push origin main
```

현재 빌드와 타입 검사는 통과한 상태입니다. 실제 서비스 전환 전에는 반드시 공개 anon RLS를 사용자별 RLS로 교체하는 작업을 우선 진행해야 합니다.
