"use client";

import Link from "next/link";
import { useParams } from "next/navigation";
import { FormEvent, useState } from "react";
import { useTripStore } from "@/lib/trip-store";

type ScheduleForm = { date: string; title: string; time: string; type: string; placeId?: string; note: string };
const control = "rounded-xl border border-[var(--color-border)] bg-[var(--color-background)] px-3 py-2.5 text-sm";

export default function SchedulePage() {
  const { tripId } = useParams<{ tripId: string }>();
  const { state, addSchedule, updateSchedule, removeSchedule } = useTripStore(tripId);
  const defaultDate = state.weather[0]?.date || new Date().toISOString().slice(0, 10);
  const empty = (): ScheduleForm => ({ date: defaultDate, title: "", time: "09:00", type: "Sightseeing", placeId: "", note: "" });
  const [item, setItem] = useState<ScheduleForm>(() => empty());
  const [editingId, setEditingId] = useState<string | null>(null);
  const dates = Array.from(new Set([...state.schedule.map((entry) => entry.date), ...state.weather.map((day) => day.date)])).filter(Boolean).sort();
  const save = (event: FormEvent) => { event.preventDefault(); if (!item.title.trim() || !item.date) return; const value = { ...item, title: item.title.trim(), note: item.note.trim() }; if (editingId) updateSchedule(editingId, value); else addSchedule(value); setItem(empty()); setEditingId(null); };
  const edit = (entry: ScheduleForm & { id: string }) => { setEditingId(entry.id); setItem({ date: entry.date, title: entry.title, time: entry.time, type: entry.type, placeId: entry.placeId ?? "", note: entry.note }); window.scrollTo({ top: 0, behavior: "smooth" }); };
  return <main className="min-h-screen px-5 py-8 pb-16 sm:px-10 lg:px-20 lg:py-12"><div className="mx-auto max-w-6xl"><Link href={`/trips/${tripId}/during`} className="text-sm font-bold text-[var(--color-primary)]">← During the trip</Link><p className="eyebrow mt-10 mb-3">Itinerary board</p><h1 className="text-4xl font-bold">Shape every day.</h1><p className="mt-2 muted">Add a date, then click any card to edit it.</p><form onSubmit={save} className="card mt-9 grid gap-3 p-5 sm:grid-cols-2 lg:grid-cols-7"><input required type="date" value={item.date} onChange={(event) => setItem({ ...item, date: event.target.value })} className={control}/><input required value={item.title} onChange={(event) => setItem({ ...item, title: event.target.value })} placeholder="What are you doing?" className={control}/><input type="time" value={item.time} onChange={(event) => setItem({ ...item, time: event.target.value })} className={control}/><select value={item.type} onChange={(event) => setItem({ ...item, type: event.target.value })} className={control}><option>Sightseeing</option><option>Food</option><option>Activity</option><option>Stay</option><option>Transport</option></select><select value={item.placeId} onChange={(event) => setItem({ ...item, placeId: event.target.value })} className={control}><option value="">No place</option>{state.places.map((place) => <option key={place.id} value={place.id}>{place.name}</option>)}</select><input value={item.note} onChange={(event) => setItem({ ...item, note: event.target.value })} placeholder="Memo" className={control}/><button className="rounded-xl bg-[var(--color-primary)] px-4 py-2.5 text-sm font-bold text-white">{editingId ? "Save" : "Add"}</button></form>{editingId && <button onClick={() => { setEditingId(null); setItem(empty()); }} className="mt-3 text-sm font-bold text-[var(--color-primary)]">Cancel editing</button>}<div className="mt-6 grid gap-4 lg:grid-cols-3">{dates.map((date) => <section className="min-h-56 rounded-[20px] border border-[var(--color-border)] bg-[var(--color-surface-muted)] p-4" key={date}><div className="flex items-center justify-between"><p className="text-sm font-bold text-[var(--color-primary)]">{date}</p><span className="text-xs muted">{state.schedule.filter((entry) => entry.date === date).length} items</span></div>{state.schedule.filter((entry) => entry.date === date).sort((a, b) => a.time.localeCompare(b.time)).map((entry) => <div className="mt-4 rounded-2xl bg-[var(--color-surface)] p-4 shadow-sm" key={entry.id}><button type="button" onClick={() => edit(entry)} className="block w-full text-left"><p className="text-xs font-bold text-[var(--color-primary)]">{entry.time || "Any time"}</p><p className="mt-2 text-sm font-bold">{entry.title}</p><p className="mt-2 text-xs muted">{entry.type} · {state.places.find((place) => place.id === entry.placeId)?.name || "No place linked"}</p>{entry.note && <p className="mt-2 text-xs muted">{entry.note}</p>}<p className="mt-2 text-[11px] font-bold text-[var(--color-primary)]">Click to edit</p></button><div className="mt-3 flex justify-end"><button type="button" onClick={() => removeSchedule(entry.id)} aria-label={`Delete ${entry.title}`} className="rounded-xl px-3 py-2 text-xs font-bold text-[var(--color-danger)]">Delete</button></div></div>)}</section>)}</div>{!dates.length && <div className="card mt-6 p-6 text-sm muted">No days planned yet. Add your first schedule item above.</div>}</div></main>;
}
